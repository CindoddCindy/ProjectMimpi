<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cindodcindy.sirihpinang.prayernote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.AnswPrayList"></activity>
        <activity android:name=".controller.EditAnsw" />
        <activity android:name=".controller.EditActivity" />
        <activity android:name=".controller.WriteAnsw" />
        <activity android:name=".view.ExpandLayout" />
        <activity android:name=".view.ListDoa" />
        <activity android:name=".controller.WriteDoa" />
        <activity android:name=".controller.LoginUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>